name: Deploy Flutter Web to Vercel

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      APP_NAME: Mi App Production
      API_URL: https://api.produccion.com
      DEBUG: false

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.27.2'  # ← VERSIÓN EXACTA QUE TIENES

    - name: Install dependencies
      run: flutter pub get

    - name: Create environment file
      run: |
        echo "APP_NAME=$APP_NAME" > assets/config/.env
        echo "API_URL=$API_URL" >> assets/config/.env
        echo "DEBUG=$DEBUG" >> assets/config/.env
        echo "✅ Environment file created:"
        cat assets/config/.env

    - name: Build Flutter Web
      run: flutter build web --release --web-renderer html

    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-args: '--prod'
        working-directory: ./